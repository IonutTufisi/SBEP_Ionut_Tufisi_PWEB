package com.sbep.SBEP.Repository;

import com.sbep.SBEP.Entity.Evenimente;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EvenimenteRepository extends JpaRepository<Evenimente, Long> {
}
